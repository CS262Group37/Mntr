name: PostgreSQL container set up
on: push

jobs:
  # Label of the container job
  container-job:
    # Containers must run in Linux based operating systems
    runs-on: ubuntu-latest
    # Docker Hub image that `container-job` executes in
    container: python

    # Service containers to run with `container-job`
    services:
      # Label used to access the service container
      postgres:
        # Docker Hub image
        image: postgres
        # Provide the password for postgres
        env:
          POSTGRES_DB: postgres_db
          POSTGRES_PASSWORD: postgres_password
          POSTGRES_PORT: 5432
          POSTGRES_USER: postgres_user
        ports:
          # maps tcp port 5432 on service container to the host
          - 5432:5432
        # Set health checks to wait until postgres has started
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    
    steps:
      - name: Install PostgreSQL client
        run: |
          apt-get update
          apt-get install --yes postgresql-client
      - name: Setup database
        run: |
          psql -h postgres -d postgres_db -U postgres_user -c 'CREATE DATABASE "cs261_project";'
          psql -h postgres -d postgres_db -U postgres_user -c '\l'
        env:
          PGPASSWORD: postgres_password
      - name: Check port
        run: echo ${{ job.services.postgres.ports['5432'] }}
      - uses: actions/checkout@v2
      - name: Create .env file
        run: |
          touch backend/.env
          echo FLASK_APP=app >> .env
          echo FLASK_ENV=development >> .env
          echo DB_USER=postgres_user >> .env
          echo DB_HOST=postgres >> .env
          echo DB_NAME=postgres_db >> .env
          echo DB_PASSWORD=postgres_password >> .env
          echo DB_DSN=postgres://postgres_user:postgres_password@postgres:5432/postgres_db >> .env
          echo HOSTNAME=http://localhost:5000 >> .env
          cat .env
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r backend/requirements.txt
      - name: Run flask server
        run: |
          cd backend
          flask run
      # - name: Run tests
      #   run: pytest
      # - name: Log in to server with postgres user
      #   run: docker run --name some-postgres -e POSTGRES_PASSWORD=mysecretpassword -d postgres
      # - name: Create user for postgres
      #   run: createuser testuser
      # - name: Create database
      #   run: createdb testdb
